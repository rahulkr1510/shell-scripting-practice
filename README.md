# shell scripting practice

This repository contains my shell scripting practice basic to command 

## Scripts 
-hello.sh 
-server_info.sh
-system_report.sh(This introduce a new style : $(command) means command output inside text)
-variable.sh(This introduce variable access with $variablename and the value will be shown)
-input.sh(how to take a input)
-args.sh(./args.sh firstargument secondargument ...)